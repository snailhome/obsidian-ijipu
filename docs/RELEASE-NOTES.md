# 爱记谱 iJipu 0.2.0（引擎同步 adj334~338，音符上方单字修饰符）

## 引擎同步

- **同步主项目 `packages/ijipu-engine` 至 adj338**（vendor/engine 镜像更新 `layout/spacing.ts`、`render/index.ts`）：
  - **adj334 音符上方单字修饰符**：新增 `&tu`（吐音→粗体 **T**）/`&ku`（吐音→粗体 **K**）/`&da`（打音→粗体 **扌**）/`&die`（叠音→粗体 **又**），写在音符后、居中显示于音符正上方（`render/index.ts` 的 `ABOVE_GLYPH`，用中文黑体栈 `FONT_CN` 粗体 + `text-anchor="middle"`）。
  - **adj335 叠音「又」横向缩 3/4**：`scale(0.75,1)`，以数字槽中心为锚缩放、视觉居中。
  - **adj336 新增吐音 `&ku`**：与 `&tu` 同显示方式（粗体 K，无缩放）。
  - **adj338 上方修饰层专属层间留白更小**：新增 `SYM_LAYER_GAP` 仅用于音符上方符号循环（共享 `LAYER_GAP=2` 仍用于减时线/低八度点/连音线），多修饰符堆叠紧凑而不影响其它层。

## 用法

完全兼容旧版（0.1.0 / 0.2.0 的 `.jps` 代码块无需改动）。详细见 `README.md`。

> 0.10.4 的发布说明未单独记录（当时引擎已随 0.10.3 同步 + 携 0.10.5 引擎），0.10.3 及更早的发布说明保留如下，未改动。

---

# 爱记谱 iJipu 0.10.3（主机功能同步 adj322 宽窄屏自动分屏）

## 主机功能同步

- **adj322 宽屏自动左右分屏 / 窄屏自动上下分屏**：本插件视图继承自主项目 `App.tsx` 的视口驱动拆分逻辑（≤1024px = 窄屏）；本插件宿主 Obsidian 自身的 split 行为独立于主机，本版本号同步标记该主机侧特性已发布。具体见主项目 `CHANGELOG.md` 0.10.3 条目。

## 用法

完全兼容旧版（0.1.0 / 0.2.0 的 `.jps` 代码块无需改动）。详细见 `README.md`。

> 0.10.2 的发布说明保留如下，未改动。

---

# 爱记谱 iJipu 0.10.2（引擎同步 adj321 → adj321b，连音线连接点修复）

## 引擎同步

- **同步主项目 `packages/ijipu-engine` 至 adj321b**（vendor/engine 镜像更新 `layout/index.ts`）：
  - **adj321 连音线连接点规则**：连音线至少连接 2 个音符。`(X)`（一个音符被 `(`开 + `)`关紧贴）且栈内还有更早未闭合连音时，X 是**连接点**——终止前一连音于 X、并以 X 为起点重新开启新连音。用例：`(1 (2) 3)` → 1→2 + 2→3（两条）；`(1(2 3))` / `(1 (2 3) 4)` 为真嵌套（内层 ≥ 2 音符，不触发连接点）。修跨行连音线：上一行 `(7,/ 6,// 5,//) (6,- |` 与下一行 `(6,)--- | 6)- ...` 的 `(6,)` 此前被 LIFO 栈式配对跳过，现正确作为连接点（上一行 6, → 连接点 + 连接点 → 下一行第二个 6,）。
  - **adj321b 深嵌套连接点豁免**：连接点检测**不受 `depth<2`（adj91 渲染层数限制）约束**。大段谱 `...(6,- | 1 2 (6,)- | 6,)- ...` 的 `(6,)` 嵌套在 `depth≥2` 括号内时，原被外层 `s.depth < 2` 阻断连接点判定为普通退化对、跳到第二个 6,；已把连接点分支移出 `depth<2`，连接点总是触发，`depth` 取被终止连音层级保持可渲染。

## 用法

完全兼容旧版（0.1.0 / 0.2.0 的 `.jps` 代码块无需改动）。详细见 `README.md`。

> 0.2.0 的发布说明保留如下，未改动。

---

# 爱记谱 iJipu 0.1.0（首版）

在 Obsidian 笔记中用 ` ```jps ` 代码块把 **.jps 简谱脚本**渲染为可视化简谱（SVG），并可**试听**。渲染引擎复用 `@ijipu/engine`（与 iJipu 应用一致），设置项与 iJipu 一脉传承。

## 主要功能

- **可视化简谱渲染**：引擎支持 `.jps` 描述头、Q/C 行、修饰符、连音线、多声部、小节线 / 反复 / 跳房子
- **试听**：Web Audio 合成（SynthBackend），播放色块逐音符跟进（iJipu 同款 playheadPosOf 逻辑），多声部同时高亮
- **三种显示模式**：整页 / 满宽 / **谱面**（默认，自动消除页边距、最大化有效观看面积）
- **设置项与 iJipu 应用一脉传承**：页面 / 字体 / 行距 / 渲染 四组共 30+ 项
- **frontmatter 覆盖**：单笔记可用 `ijipu_*` 键覆盖全局默认，优先级：默认 < 全局 < frontmatter
- **插件卸载 / 切笔记自动停止试听**：避免失去控制
- **赞赏入口**：设置页「支持作者 ❤」链接到 iJipu 应用微信赞赏码

## 用法

在任意笔记写 ` ```jps ` 代码块即可：

```jps
V: 1.0
B: 两只老虎
Z: 法国童谣 词曲
D: C
P: 4/4
Q: 1 2 3 1 | 1 2 3 1 | 3 4 5 - | 3 4 5 - |
C: 两只老虎 两只老虎 跑得快 跑得快
```

详细 frontmatter 键对照表见 `README.md`。

## 链接

- [iJipu 官网](https://ijipu.pages.dev)
- [脚本规则说明](https://ijipu.pages.dev/doc/jps-spec.html)
- 引擎：[@ijipu/engine](https://github.com/snailhome/obsidian-ijipu)（vendor 内置，仓库自包含）

## 反馈

有问题请到 iJipu 官网或 GitHub Issues。

<!-- 本文件只保留**当前版本**的更新记录；历史版本见 docs/RELEASE-NOTES-ARCHIVE.md。
     GitHub Release 正文由 scripts/release-body.mjs 从本文件截取当前版本。

     发版流程（每次）：
     1) 写本节（H1 必须形如 `# 爱记谱 iJipu <manifest.json 的 version>`，脚本按版本号定位）；
     2) 把**上一版**的内容整段移到 docs/RELEASE-NOTES-ARCHIVE.md（本文件只留当前版本）；
     3) 同步 manifest.json / package.json / versions.json（+ package-lock.json）的版本号；
     4) 提交 → 打 tag → 推送；CI 会用 release-body.md（只含当前版本）作为 Release 正文。 -->

# 爱记谱 iJipu 0.10.1

> 本版只做一件事：**同步 `@ijipu/engine`（0.13.1 → 0.13.2）**，带来两处谱面渲染 / 格式化修复。
> 插件自身代码无改动。

## 同步的引擎修复

- **跨行连音线端点弧线不统一**（`adj459`）：音符比较靠近左边距时，结束弧线会明显比别处小。
  半径改为**同时受「层级间距」与「两端水平距离」约束**（单音符取 `|Δx| - 1`、多音符取 `|Δx| / 2 - 1`），
  于是横向长短不一的连音线，弧线**形状统一**。
- **`+` / `-` 变号未断开**（`adj460`）：`5"注释"++++---` 现在格式化为 `5"注释"++++ ---`
  （同向追加不断开、**变号处断开**）；连音线前缀 `(y…` 内部不插空格。

## 说明

- 语法与语义**零变化**，`docs/SYNTAX.md` 无需改动；排版不位移（只是弧线 / 空格的呈现修正）。
- 应用侧同版本（iJipu `0.24.0`）另含「底部 dock：告警 / 试听 / 导出 / 设置四面板合一」，
  属应用 UI，与本插件无关。
